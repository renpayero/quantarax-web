# Cómo levantar la App Rápido 🚀

1.  **Instalar dependencias** (si es la primera vez):

    ```bash
    npm install
    ```

2.  **Iniciar el servidor de desarrollo**:

    ```bash
    npm run dev
    ```

3.  **Abrir en el navegador**:
    - La app correrá en: [http://localhost:3000](http://localhost:3000)

---

**Comandos útiles:**

- `npm run build`: Para construir la versión de producción.
- `npm run start`: Para correr la versión construida.
